[task]
name = "fred-mcp/unemployment-latest"
description = "What is the most recent US unemployment rate (series UNRATE), as a percent?"

[metadata]
suite = "fred-mcp"

[environment]
docker_image = "fred-bench"
network_mode = "public"

[agent]
timeout_sec = 300

[verifier]
timeout_sec = 60
