[task]
name = "fred-mcp/series-units"
description = "What are the units of the real GDP series GDPC1? Report the full units description exactly as FRED gives it."

[metadata]
suite = "fred-mcp"

[environment]
docker_image = "fred-bench"
network_mode = "public"

[agent]
timeout_sec = 300

[verifier]
timeout_sec = 60
