[task]
name = "fred-mcp/release-series-count"
description = "The Employment Situation release has FRED release id 50. Count how many of the series it publishes are monthly."

[metadata]
suite = "fred-mcp"

[environment]
docker_image = "fred-bench"
network_mode = "public"

[agent]
timeout_sec = 300

[verifier]
timeout_sec = 60
